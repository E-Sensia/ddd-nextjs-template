# Server
PORT=3000

# Logging (log-format doctrine: json one-per-line in prod, pretty in dev)
LOG_LEVEL=info
# LOG_FORMAT overrides the NODE_ENV-based default: json | console
#LOG_FORMAT=console

# OpenTelemetry
OTEL_SERVICE_NAME=my-service
APP_VERSION=0.1.0
